diff a/src/components/pub_commentary_upload.vue b/src/components/pub_commentary_upload.vue	(rejected hunks)
@@ -89,8 +89,10 @@
       if (!isLt2M) {
         this.$message.error(`图片大小不能超过${this.fileSize}MB！`);
       }
+      let postData = new FormData();
+      postData.append("file", file);
       if (typeStatus && isLt2M) {
-        return getUploadSign({ menu: "saas-dir" }).then(res => {
+        return getUploadSign(postData).then(res => {
           let filePath = this.getFileName(file.name, res.data.dir);
           this.postData = {
             key: filePath,
